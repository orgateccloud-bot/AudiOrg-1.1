apiVersion: v1
kind: Service
metadata:
  name: {{ include "orgaudi.fullname" . }}-frontend
  labels:
    {{- include "orgaudi.labels" . | nindent 4 }}
    component: frontend
spec:
  type: {{ .Values.frontend.service.type }}
  ports:
  - port: {{ .Values.frontend.service.port }}
    targetPort: http
    protocol: TCP
    name: http
  selector:
    {{- include "orgaudi.selectorLabels" . | nindent 4 }}
    component: frontend
