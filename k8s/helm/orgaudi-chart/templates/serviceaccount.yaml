{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "orgaudi.serviceAccountName" . }}
  labels:
    {{- include "orgaudi.labels" . | nindent 4 }}
{{- end }}
