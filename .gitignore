# Baseline local-only ignores
.env
.env.*
!.env.example
!.env.*.example
*.pem
*.key
*.p12
*.pfx
credentials.json
id_rsa

# Python
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/
.venv/
venv/
env/

# Node
node_modules/
.next/
dist/
build/
coverage/

# Editor and local tooling
.idea/
.vscode/
.cursor/
.claude/
.uix/

# Local artifacts
*.db
*.db-shm
*.db-wal
*.sqlite
*.sqlite3
*.pkl
*.joblib
*.log
tmp/
.cache/






# Credenciais e config local
config.env

# Outputs gerados (relatórios, laudos, extrações)
relatorios/
data/laudos/
*.xlsx
!src/**/*.xlsx

# Logs
extractor.log
.coverage


# Runtime outputs gerados (relatórios, simulações, ledger JSONL)
out/
reports_nfa/*.json
reports_nfa/*.md
reports_nfa/*.txt
reports_nfa/laudos_pdf/

# Laudos por cliente gerados pelo pipeline (auditoria_cruzada.json, laudos PDF, etc.)
outputs/

# PDFs e logs avulsos na raiz (artefatos de execução manual)
/LAUDO_*.pdf
/auditoria_log.txt
*.log
output_relatorios/

# Dados de cliente com PII (CPF, IE, nome) — só o modelo entra no git
scripts/clientes/*.json
!scripts/clientes/_modelo_cliente.json

# PDFs de entrada de contribuintes (PII)
data/*.pdf
input_pdfs/

# Relatórios PDF/HTML gerados em reports_nfa
reports_nfa/*.pdf
reports_nfa/*.html
